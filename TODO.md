# TODO

## Follow-up product improvements

- Add an unsafe-request warning in the UI when local safety checks flag a request as unsafe. The message should clearly say that AI Request Triage has its own local eval/safety-check system and that it marked the request unsafe, so human review is required before any customer-facing commitment or software interruption.
- Add a lightweight sort control to the Head of Software Queue so reviewers can sort requests by urgency.
